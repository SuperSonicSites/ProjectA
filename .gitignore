# OS
.DS_Store
Thumbs.db
Desktop.ini

# Node
node_modules/
npm-debug.log
yarn-debug.log
yarn-error.log

# Hugo
public/
resources/_gen/
.hugo_build.lock

# Cloudflare / Wrangler
.wrangler/
.dev.vars

# Environment Variables & Secrets
.env
.env.*
!.env.example
**/.env
**/.env.*
*.key
*.pem
secrets/
.secrets/

# Generated Assets (Tailwind)
# We generate this on build, so no need to commit unless you want to.
/static/css/main.css

# Local CMS Backend (completely local, not for repo)


# CMS Prompt Configurations (generation settings, not needed in repo)
config/prompts/

# Autonomy runtime manifests / logs (do not commit)
scripts/morning-routine/.runs/