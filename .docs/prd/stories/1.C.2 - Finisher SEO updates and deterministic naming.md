# 1.C.2 — Finisher: SEO updates + deterministic naming

Epic: `1 - PaperPause Autonomy System`

## Goal
Ensure post-QA finishing produces stable filenames/slugs and updates only allowed frontmatter fields.

## Scope
- Use `.agents/seo-copywriter/` to generate SEO fields (title/description/pinterest/slug/alt text).
- Runner enforces deterministic constraints:
  - slug normalization
  - collision handling
  - bounded frontmatter merge (no touching unrelated fields)

## Acceptance criteria
- Finisher never deletes or corrupts critical URLs (`image_url`, `download_url`, `r2_original`).
- Re-running Finisher is idempotent for a given AssetID.
- Finisher remains best-effort (a single failure does not fail the whole run, unless explicitly configured).
- **Model Standard**: `gemini-3-flash-preview` (Thinking: **MEDIUM**).


